import { styled } from '@mui/system';
import { Box } from '@mui/material';

const FlewBetween = styled(Box)({
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
});

export default FlewBetween;
